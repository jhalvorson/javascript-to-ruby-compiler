{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn install; lerna bootstrap",
    "start:web": "lerna run --scope @js-to-ruby/web --stream dev"
  }
}